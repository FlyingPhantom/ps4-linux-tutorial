name: Blank Workflow

on: 
  push:
    branches:
      - main  # or whatever branch

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow does absolutely nothing."
